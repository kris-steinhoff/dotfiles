{{- $generous := eq (default "conservative" (get . "agentBudget")) "generous" -}}
model = "{{ if $generous }}gpt-5.6-sol{{ else }}gpt-5.6-terra{{ end }}"
model_reasoning_effort = "{{ if $generous }}high{{ else }}medium{{ end }}"
sandbox_mode = "workspace-write"

developer_instructions = """
{{ template "agents/personas/chief-of-staff.md" . -}}
"""

[sandbox_workspace_write]
writable_roots = ["{{ .chezmoi.homeDir }}/.local/state/chief-of-staff"]
